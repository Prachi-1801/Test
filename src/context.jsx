import { createContext } from "react";

export const UserDetailsContext = createContext(null);
export const ConnectionContext = createContext(null);
export const UserNamesContext = createContext(null);
